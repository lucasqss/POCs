services:
  oracle-db:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: oracle23c
    environment:
      - ORACLE_PASSWORD=cenha
    ports:
      - "1521:1521"
      - "5500:5500"
    volumes:
      - oracle_data:/opt/oracle/oradata

volumes:
  oracle_data: